services:
  - type: web
    name: my-roblox-ml-server-2026  # <- 여기에 본인만의 독특한 이름을 적어주세요!
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    autoDeploy: true
